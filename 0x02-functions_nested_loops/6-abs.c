#include "main.h"
#include <atdio.h>
/**
 * _abs - function that computes the absolute value of an integer
 * @n: absolute value of integer
 * Return: The value the inputed integer to be computed
 */
int _abs(int n)
{
	if (n = 1)
	{
		printf(abs(n));
		return (-n);
	}
	else
		return(n);
}
